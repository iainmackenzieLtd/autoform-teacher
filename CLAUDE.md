# claude-code-sandbox

A learning sandbox for exploring Claude Code.

## Rules

- No package installs (`pip`, `npm`, `apt`, etc.)
- No system changes outside this folder
- Always explain what you are about to do before editing any file
- Always run `git diff` after any edit and show the output
- Never push to remote without explicit instruction
- Ask before deleting any file
